{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "args": ["how does the calculator render results to the console?"],
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
